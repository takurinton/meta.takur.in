{
  "name": "meta-tags",
  "version": "1.0.0",
  "description": "BFF for takurinton",
  "main": "index.js",
  "scripts": {
    "build": "",
    "watch": "esbuild app.js --bundle --outfile=dist/index.js --platform=node --watch"
  },
  "author": "takurinton",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "jsdom": "^20.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "esbuild": "^0.16.12"
  }
}
